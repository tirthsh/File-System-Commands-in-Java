package fileSystem;
import javax.swing.JTree;
import javax.swing.tree.DefaultMutableTreeNode;

public class fileSystem {
  private JTree fs;
  public DefaultMutableTreeNode file;
  public fileSystem(){
    // Initialize with the home directory
    this.file = new DefaultMutableTreeNode("Home");
    this.fs = new JTree(this.file);
  }
  // adds new file to filesystem.
  public void add(){
    
  }
  // removes specified file from the filesystem
  public void remove(){
    
  }
}
